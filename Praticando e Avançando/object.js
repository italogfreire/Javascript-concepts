// Object

const person = {
  name: 'John',
  age: 30,
  weight: 80,
  isAdmin: true
}

// Para pegarmos apenas uma propriedade, utilizamos o "."

console.log(`${person.name} tem ${person.age} anos`);